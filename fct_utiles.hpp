#ifndef FCT_UTILES_HPP_INCLUDED
#define FCT_UTILES_HPP_INCLUDED

#include <string>
#include <vector>
#include <map>

#include "class_tagInfos.hpp"

void clearLine(int nChar);	// When using the '\r', to delete all previous characters

struct songInfos genVoidStructSongInfos();
struct audioProperties genVoidStructAudioProperties();


#endif
